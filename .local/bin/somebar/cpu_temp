#!/bin/bash
sensors | awk '/CPU/ {gsub(/\+|ºC/,"",$2); print $2; exit}' # dell
# sensors | awk '/Package id/ {gsub(/\+|ºC/,"",$4); print $4; exit}' # intel
# sensors | awk '/Tctl/ {gsub(/\+|ºC/,"",$2); print $2; exit}' # amd
