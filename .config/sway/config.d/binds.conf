###################
### KEYBINDINGS ###
###################

set $mod Mod4

set $terminal alacritty
set $browser firefox
set $fileManager thunar

set $scripts "~/.scripts"
set $next swaymsg -r -t get_workspaces | jq -r --arg OUTPUT $(swaymsg -t get_outputs -r | jq -r '.[] | select(.focused == true) | .name') '(. | (max_by(.num) | .num)) as $max | [.[] | select(.output == $OUTPUT)] | (max_by(.num) | .num) as $maxOutput | (.[] | select(.focused == true) | .num) as $current | if $maxOutput > $current then "next_on_output" else $max + 1 end'
set $prev swaymsg -r -t get_workspaces | jq -r --arg OUTPUT $(swaymsg -t get_outputs -r | jq -r '.[] | select(.focused == true) | .name') '(. | (max_by(.num) | .num)) as $max | [.[] | select(.output == $OUTPUT)] | (min_by(.num) | .num) as $minOutput | (.[] | select(.focused == true) | .num) as $current | if $minOutput < $current then "prev_on_output" else $current end'

# drag floating windows by holding down $mod and left mouse button.
# resize them with right mouse button + $mod.
# despite the name, also works for non-floating windows.
# change normal to inverse to use left mouse button for resizing and right
# mouse button for dragging.
floating_modifier $mod normal

# kill focused window
bindsym $mod+C kill

# exit sway
bindsym $mod+Shift+Escape exec swaymsg exit

# reload sway configuration file
bindsym $mod+Alt+Escape reload

# move focus around
bindsym $mod+H focus left
bindsym $mod+J focus down
bindsym $mod+K focus up
bindsym $mod+L focus right

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused container around workspace
bindsym $mod+CTRL+H move left
bindsym $mod+CTRL+J move down
bindsym $mod+CTRL+K move up
bindsym $mod+CTRL+L move right

bindsym $mod+CTRL+Left move left
bindsym $mod+CTRL+Down move down
bindsym $mod+CTRL+Up move up
bindsym $mod+CTRL+Right move right

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# swtich through existing workspaces
bindsym $mod+ALT+H  exec swaymsg "workspace $($prev)"
bindsym $mod+ALT+L  exec swaymsg "workspace $($next)"
bindsym $mod+ALT+Left   exec swaymsg "workspace $($prev)"
bindsym $mod+ALT+Right  exec swaymsg "workspace $($next)"

# move focused container to workspace
bindsym $mod+SHIFT+1 move container to workspace 1; workspace 1
bindsym $mod+SHIFT+2 move container to workspace 2; workspace 2
bindsym $mod+SHIFT+3 move container to workspace 3; workspace 3
bindsym $mod+SHIFT+4 move container to workspace 4; workspace 4
bindsym $mod+SHIFT+5 move container to workspace 5; workspace 5
bindsym $mod+SHIFT+6 move container to workspace 6; workspace 6
bindsym $mod+SHIFT+7 move container to workspace 7; workspace 7
bindsym $mod+SHIFT+8 move container to workspace 8; workspace 8
bindsym $mod+SHIFT+9 move container to workspace 9; workspace 9
bindsym $mod+SHIFT+0 move container to workspace 10; workspace 10

# move focused container to next or previous workspace
bindsym $mod+SHIFT+ALT+H exec swaymsg "move container to workspace $($prev), workspace prev_on_output"
bindsym $mod+SHIFT+ALT+L exec swaymsg "move container to workspace $($next), workspace next_on_output"
bindsym $mod+SHIFT+ALT+Left exec swaymsg "move container to workspace $($prev), workspace prev_on_output"
bindsym $mod+SHIFT+ALT+Right exec swaymsg "move container to workspace $($next), workspace next_on_output"

# split
bindsym $mod+SHIFT+Z splith
bindsym $mod+ALT+Z splitv

# switch the current container between different layout styles
bindsym $mod+Z layout toggle split
bindsym $mod+W layout tabbed
bindsym $mod+G layout stacking

# make the current focus fullscreen
bindsym $mod+F fullscreen

# toggle the current focus between tiling and floating mode
bindsym $mod+X floating toggle

# swap focus between the tiling area and the floating area
bindsym $mod+SPACE focus mode_toggle

# move focus to the parent container
bindsym $mod+SHIFT+X focus parent

# scratchpad
bindsym $mod+S          scratchpad show
bindsym $mod+SHIFT+S    move scratchpad

# resizing containers
bindsym $mod+SHIFT+H resize grow width 10px
bindsym $mod+SHIFT+J resize grow height 10px
bindsym $mod+SHIFT+K resize shrink height 10px
bindsym $mod+SHIFT+L resize shrink width 10px

bindsym $mod+SHIFT+Left resize grow width 10px
bindsym $mod+SHIFT+Down resize grow height 10px
bindsym $mod+SHIFT+Up resize shrink height 10px
bindsym $mod+SHIFT+Right resize shrink width 10px

# take screenshot
bindsym Print exec grim

# basicos
bindsym $mod+Q exec $terminal
bindsym $mod+B exec $browser
bindsym $mod+E exec $fileManager

# rofi
bindsym $mod+R exec "killall rofi; rofi -show drun -theme style.rasi"
bindsym $mod+PERIOD exec "killall rofi; rofi -modi emoji -show emoji -theme style.rasi -theme-str 'window { width: 500px; }'"
bindsym $mod+SHIFT+PERIOD exec "killall rofi; ~/.config/rofi/scripts/rofi-kaomojis"

# brightness
bindsym $mod+F4 exec "brightnessctl set 5%-; $scripts/brightget"
bindsym $mod+F8 exec "brightnessctl set 5%+; $scripts/brightget"
bindsym --locked XF86MonBrightnessDown exec "brightnessctl set 5%-; $scripts/brightget"
bindsym --locked XF86MonBrightnessUp exec "brightnessctl set 5%+; $scripts/brightget"

# volume
bindsym $mod+F1 exec "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle; $scripts/volumeget"
bindsym $mod+F2 exec "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-; $scripts/volumeget"
bindsym $mod+F3 exec "wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+; $scripts/volumeget"

bindsym $mod+SHIFT+F1 exec "$terminal --class pulsemixer -e 'pulsemixer'"
bindsym $mod+SHIFT+F2 exec "wpctl set-volume @DEFAULT_AUDIO_SINK@ 10%-; $scripts/volumeget"
bindsym $mod+SHIFT+F3 exec "wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 10%+; $scripts/volumeget"

bindsym $mod+CTRL+F2 exec "wpctl set-volume @DEFAULT_AUDIO_SINK@ 1%-; $scripts/volumeget"
bindsym $mod+CTRL+F3 exec "wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 1%+; $scripts/volumeget"

bindsym --locked XF86AudioMute exec "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle; $scripts/volumeget"
bindsym --locked XF86AudioLowerVolume exec "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-; $scripts/volumeget"
bindsym --locked XF86AudioRaiseVolume exec "wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+; $scripts/volumeget"
bindsym --locked XF86AudioMicMute exec "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"

# mpd / mpc
bindsym $mod+F5 exec "mpc toggle"
bindsym $mod+F6 exec "mpc volume -10; $scripts/volumeget-mpd"
bindsym $mod+F7 exec "mpc volume +10; $scripts/volumeget-mpd"
bindsym $mod+SHIFT+F5 exec "mpc stop"
bindsym $mod+SHIFT+F6 exec "mpc prev"
bindsym $mod+SHIFT+F7 exec "mpc next"

# outros
bindsym $mod+T exec swaync-client -t
bindsym $mod+ESCAPE exec $scripts/wlogouttoggle
bindsym $mod+CTRL+B exec $scripts/waybartoggle
bindsym $mod+V exec "killall clipse; $terminal --class clipse -e 'clipse'"
bindsym $mod+CTRL+F1 exec blueman-manager
bindsym $mod+ALT+F1 exec "killall nmtui; $terminal --class nmtui -e 'nmtui'"
bindsym $mod+HOME exec $scripts/wall
